#include <stdio.h>

int main()
{
    char str[100],i,len=0;
    printf("enter a string:");
    scanf("%s",&str);
    for(i=0;str[i] != '\0';i++){
        len++;
    }
    printf("the length of the string is %d",len);
    return 0;
}
