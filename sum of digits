
#include <stdio.h>
int main()
{
    int i,sum=0,n,temp;
    printf("enter a number:");
    scanf("%d",&n);
    
    while(n!=0)
    {
        temp=n%10;
        sum=sum+temp;
        n=n/10;

    }
    printf(" the sum of digit is %d ",sum);
    return 0;
}
