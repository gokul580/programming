#include <stdio.h>
int main()
{
    int rev,pal=0,n;
    printf("enter a number:");
    scanf("%d",&n);
    while(n!=0){
        rev=n%10;
        pal=pal*10+rev;
        n=n/10;
        
    }
    printf("the reverse of the number is %d",pal);
    return 0;
    
}
